設定用スプレッドシートで編集してください。
この設定ページでは設定用スプレッドシートの参照のみを行えます。

店舗：
参照するURLを変更したい店舗名を選択します。

シートURL：
スプレッドシートのURLをそのまま貼り付けます。

基本時給：
計算時に自動で設定される時給を入力します。

時間外倍率：
一日あたりの連続勤務時間が8時間を超えた分に対して加算される倍率です。

従業員名除外ワード：
自動で従業員名を抽出しますが、店舗独自に追加した列名(例：メモ)には適用されません。
手動で除外ワードを設定する必要があります。
